name: CICD
on: [push]
jobs:
  build:
    runs-on: tpcrunner1
    strategy:
      matrix:
        go: [ '1.13' ]
    name: Go ${{ matrix.go }} sample
    steps:
      - name: Setup go
        uses: actions/setup-go@v2
        with:
          go-version: ${{ matrix.go }}
